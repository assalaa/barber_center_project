import 'package:flutter/material.dart';

class OnlineStatusBar extends StatelessWidget {
  const OnlineStatusBar({super.key});

  @override
  Widget build(BuildContext context) {
    // TODO(assala): make a online/offline ligth
    return AppBar();
  }
}
